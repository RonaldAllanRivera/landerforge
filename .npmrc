# pnpm's default isolated node_modules is the point: it surfaces phantom
# dependencies at install time instead of at deploy time.
strict-peer-dependencies=false
# next build traces files out of the store; hoisting these keeps that resolvable.
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
